apiVersion: apps/v1
kind: Deployment
metadata:
  name: maven-app
spec:
  selector:
    matchLabels:
      app: maven-app
  replicas: 2

  template:
    metadata:
      labels:
        app: maven-app
    spec:
      containers:
      - name: maven-app
        image: bjgomes/maven
        ports:
        - containerPort: 8080
